# Acoustic Atelier – Python 3.14 (Optimized for macOS)
# Mac Apple Silicon (M-series) / MPS-optimized
#
# Install:
#   uv venv --python 3.14 && source .venv/bin/activate
#   uv pip install -r requirements.txt
#
# NOTE: If Python 3.14 wheels are missing for your architecture,
# you can use Python 3.12 as a stable alternative.

# ── TTS backend ──────────────────────────────────────────────────────────────
-e ./third_party/Qwen3-TTS

# ── PyTorch (Apple-MPS build – pre-installed via pip or conda) ──────────────
# If not already installed:
#   uv pip install torch torchaudio
torch>=2.10.0
torchaudio>=2.10.0

# ── Audio I/O & processing ───────────────────────────────────────────────────
soundfile>=0.13.1
numpy>=1.26.4,<2.0.0   # keep <2 for MPS stability (confirmed working)
scipy>=1.17.1
torchcodec>=0.10.0

# ── GUI ───────────────────────────────────────────────────────────────────────
flet>=0.80.5

# ── Transkription (Whisper) ───────────────────────────────────────────────────
# Runs on CPU (MPS support in Whisper still unstable)
openai-whisper>=20250625

# ── Data Miner ───────────────────────────────────────────────────────────────
speechbrain>=1.0.3          # Speaker verification (ECAPA-TDNN)
google-genai>=1.64.0        # Gemini API for emotion labelling (optional)
